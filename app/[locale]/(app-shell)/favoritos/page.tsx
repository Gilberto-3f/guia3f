'use client'

/**
 * Página de favoritos (exclusiva do turista na barra inferior).
 * Shell vazio — conteúdo será refatorado em etapa seguinte.
 */
export default function FavoritosPage() {
  return (
    <div className="flex min-h-0 flex-1 flex-col bg-gray-50">
      <header className="shrink-0 border-b border-white/15 bg-[#0097b2] px-4 py-4 pt-safe">
        <h1 className="text-center text-lg font-bold uppercase tracking-wide text-white">
          Favoritos
        </h1>
      </header>
      <main className="flex min-h-0 flex-1 flex-col items-center justify-center px-6 py-10">
        {/* Conteúdo a ser implementado */}
      </main>
    </div>
  )
}
