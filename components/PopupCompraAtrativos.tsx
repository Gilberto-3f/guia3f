'use client'

/**
 * Compat: o fluxo de tickets passou de popup coletivo para drawers
 * (lista → detalhe → pagamento). Preferir `DrawerTicketsAtrativos`.
 */
export { default } from '@/components/DrawerTicketsAtrativos'
